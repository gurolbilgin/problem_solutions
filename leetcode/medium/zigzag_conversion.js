/*
 * @param {string} s
 * @param {number} numRows
 * @return {string}
 */

const convert = function (s, numRows) {
  const charCount = 0
  const arrayCount = 0
  for (let i in s) {
    charCount++
  }
  
};
